# Drop & Bounce Path — Kill Switch

Public JSON for remote kill switch: `{"enabled": true|false}`.

- `true` — app runs
- `false` — app blocked

URL after Pages: `https://<USER>.github.io/plinko_kill_switch/kill-switch.json`
